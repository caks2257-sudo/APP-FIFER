/**
 * Alias del sidebar del dashboard. El layout usa `SidebarAuto`; este módulo
 * mantiene el nombre `DashboardSidebar` para imports y documentación.
 */
export { SidebarAuto as DashboardSidebar } from "@/components/core/SidebarAuto";
