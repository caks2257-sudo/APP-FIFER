/**
 * Ruta canónica Vault / Perfil (credenciales, BYOK, re-OAuth).
 * Alineado con `04_INTEGRATIONS_HEALTH.md` y el dashboard `[module]/[...submodule]`.
 */
export const FIFER_VAULT_PROFILE_HREF = "/dashboard/vault";
