UI: Grid12, #0A0F1E/#EAB308, r:0.75rem, gap:gap-4, side:280/80, z:40/50/60, resp:1|6|12, tailwind-only(no CSS modules/CSS-in-JS), biomeVars(--fifer-primary/--fifer-accent/--fifer-deep-navy).
LOGIC: DualStage(isRefining->Success), JIT-Hydration(BoxLoader->getV0BoxLoader), StateFlow(idle|loading|error|ghost|locked|drag), ErrorIso(BoxErrorBoundary->DiscoveryBox), CircuitBreaker(threshold->open->heal), Ghost(no-data/fetch/jit-fail), WalletGate(aiBlocked->TopUp), CatalogGap(dev banner).
SHELL: BoxProps{data,config,isLoading,error,isLocked,isRefining,boxId}, SDUI config types(table|chart|form), LayoutStore sanitize(boxId whitelist + grid clamp), Commander(parse->layout ops), Manifest(boxId/sourceModule/targetSlot/layout/themeOverrides/permissions).
SEC: Proxy(Vault->Provider), NoClientKeys, AIVault.resolveKey(provider,userId?) -> BYOK(TODO DB) else ENV(FIFER_ADMIN_OPENAI_KEY|LEONARDO|ELEVENLABS) else VaultError(missing_key), ai-proxy zod-guard + payload object-only + provider allowlist + error redaction([redacted], strip sk-/Bearer/env names).
AI-PIPE: AiFactory(taskType:text|image|audio)->OpenAI/Leonardo/ElevenLabs, UniversalAdapter envelope{ok,status,provider,taskType,data,error}, degraded fallback on provider fail, controller resolves taskType from action(image|generation|speech|audio|tts else text).
ROUTES: FE api base=NEXT_PUBLIC_FIFER_API_BASE_URL; secure endpoint POST /api/v1/master/ai/proxy; v0-ingestion forbidden direct external AI calls.
MODS: Fin(UF/Chicureo, biome:#059669/#D97706, slots:main|stats), Cont(ABKupfer, biome:#1E3A5F/#2563EB, slots:main|gallery), Aff(Comms, biome:#EAB308/#2563EB, slots:hero|stats|main-content).
BOXCAT: finance-cashflow-chart(6x2,refining,pro,chat), content-ingestion-form(6x3,refining,free,chat), affiliate-hero-summary(6x2,no-refining,free,chat); ext loaders map aliases (content-google-shopping->content-pipeline).
DATA-NORM: adapters enforce schema + graceful fallback; validation error => FiferDataValidationError => DiscoveryBox; finance UF card fetches proxy(action:get_current_uf) + parse value + throw on error for shell recovery path.
DOC-GOV: source-of-truth master-blueprint + xray protocol + FIFER_BRAIN index; notary-close sync: new .tsx -> 05_UI_INVENTORY, data/api shape changes -> 04_DATA_MOCKS, new design law -> 02_MASTER_STYLE only if explicit approval.
