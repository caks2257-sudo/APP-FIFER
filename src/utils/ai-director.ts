/**
 * Dual-Memory Controller y AI Director — implementación en el frontend Next (`fifer-landing`).
 * Re-export para alinear la ruta `src/utils/ai-director.ts` con el monorepo (Paso 2).
 */
export * from "../../fifer-landing/src/utils/ai-director";
