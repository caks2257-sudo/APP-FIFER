# Persistencia — system-engine:env-manager

## UBICACIÓN LÓGICA

`FIFER://engines/system-engine/sub-engines/env-manager`

**targetAppOrEngine:** `system-engine:env-manager`

**N/A (Prisma):** este sub-motor no define tablas. El archivo `.env` es el único almacén que manipula, y solo en `development`.
