# Persistencia — system-engine (raíz)

Persistencia: **N/A**. Depende del `schema.prisma` global o de la delegación a sub-motores (p. ej. `env-manager` no persiste secretos en disco más allá de flujos de desarrollo documentados en su plano).
