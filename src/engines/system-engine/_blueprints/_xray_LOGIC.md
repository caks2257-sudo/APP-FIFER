# Lógica — system-engine

## UBICACIÓN LÓGICA

`FIFER://engines/system-engine`

**targetAppOrEngine:** `system-engine`

Contenedor de Sub-Engines de sistema (p. ej. `system-engine:env-manager` para `.env` solo en desarrollo). Sin lógica de negocio de producto.
