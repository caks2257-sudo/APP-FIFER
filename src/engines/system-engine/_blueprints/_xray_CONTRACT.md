# Contrato — system-engine (raíz)

Contenedor de sub-motores de sistema (p. ej. `env-manager`). No expone contrato HTTP único; cada sub-engine define su contrato en su carpeta `_blueprints/_xray_CONTRACT.md`.

- **Registro:** `EngineRegistry` id `system-engine` (ver `_xray_LOGIC.md`).
