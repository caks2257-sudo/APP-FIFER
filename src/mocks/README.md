# `src/mocks/` (monorepo raíz)

Las **semillas TypeScript** del Living OS (contrato `BoxProps.data` + `_xray_PROTOCOL_SHELL.md`) viven en:

**[`fifer-landing/src/mocks/`](../fifer-landing/src/mocks/)**

Usa el alias **`@/mocks/*`** dentro del proyecto Next (`fifer-landing`).
