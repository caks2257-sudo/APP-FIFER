# Plano ROUTING — Afiliados

## Ruta pública

- **Path:** `/afiliados`
- **Implementación App Router:** `src/app/(dashboard)/afiliados/page.tsx` (el segmento `(dashboard)` no aparece en la URL).

## Navegación lateral

- En `src/registry/app-registry.ts`, entrada `appRegistry` con `id: 'afiliados'`, `href: '/afiliados'`, y nodo en `sidebarNavigationSource` con `appId: 'afiliados'` para **Red de Afiliados**.
